version: "2"

syncs:
  gql-poc3-match:
    src: './match'
    sync_excludes_type: 'none'
    sync_strategy: 'native_osx'
    sync_excludes: ['Name node_modules']
    notify_terminal: true
    monit_enable: true

  gql-poc3-evoke:
    src: './evoke'
    sync_excludes_type: 'none'
    sync_strategy: 'native_osx'
    sync_excludes: ['Name node_modules']
    notify_terminal: true
    monit_enable: true

  gql-poc3-campaigns:
    src: './campaigns'
    sync_excludes_type: 'none'
    sync_strategy: 'native_osx'
    sync_excludes: ['Name node_modules']
    notify_terminal: true
    monit_enable: true

  gql-poc3-gateway:
    src: './gateway'
    sync_excludes_type: 'none'
    sync_strategy: 'native_osx'
    sync_excludes: ['Name node_modules']
    notify_terminal: true
    monit_enable: true
